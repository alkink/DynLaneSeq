_base_: culane_s0_structured_query_dla34_v5_protected_ownership_base.yaml

output_dir: outputs/diagnostics/unified_lane_set_v5_a_protected_ownership_sidecar_25k

# Causal control: ownership is persistent and deeply supervised, but it cannot
# influence Hungarian.  Geometry therefore sees exactly geometry-only matches.
matcher:
  lambda_obj: 0.0
  lambda_obj_start: 0.0
  lambda_obj_end: 0.0
