_base_: culane_v30_joint_slot_field_35k_route_residual_off.yaml

output_dir: outputs/diagnostics/v31_selection_bridge_exact_pair_35k_to50k/train/field_only

training:
  # Resume begins at global iteration 35K. The locked decision endpoint is 50K.
  max_iters: 15000
  checkpoint_interval: 5000
  save_last_alias: false

dataloader:
  resume_safe: true
