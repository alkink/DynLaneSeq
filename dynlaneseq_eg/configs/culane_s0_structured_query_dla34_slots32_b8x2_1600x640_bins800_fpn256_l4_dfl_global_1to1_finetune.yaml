_base_: culane_s0_structured_query_dla34_slots32_b8x2_1600x640_bins800_fpn256_l4_dfl_50ep.yaml

# Diagnostic continuation from the historical 225k DLA-34 checkpoint.
# Only target assignment changes: each GT is positive for exactly one of the
# 32 predictions instead of once per four isolated query groups.
output_dir: outputs/diagnostics/culane_dla34_225k_global_1to1_finetune

matcher:
  assignment: hungarian
  num_groups: 1

training:
  vis_interval: 1000000
